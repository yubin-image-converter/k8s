apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - sealed-secret.yaml

patchesStrategicMerge:
  - patch-env.yaml

images:
  - name: shinyubin/image-converter-api
    newTag: latest

namespace: api

nameSuffix: -dev
