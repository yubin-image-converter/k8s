apiVersion: apps/v1
kind: Deployment
metadata:
  name: rabbitmq-dev
spec:
  template:
    spec:
      containers:
        - name: rabbitmq
          envFrom:
            - secretRef:
                name: rabbitmq-secret-dev
