apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

nameSuffix: -dev

resources:
  - ../../base
  - sealed-secret.yaml

generatorOptions:
  disableNameSuffixHash: true

patchesStrategicMerge:
  - patch-env.yaml
