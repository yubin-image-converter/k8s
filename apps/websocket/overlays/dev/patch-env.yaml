apiVersion: apps/v1
kind: Deployment
metadata:
  name: websocket-server
  namespace: image-converter
spec:
  template:
    spec:
      containers:
        - name: websocket
          envFrom:
            - secretRef:
                name: websocket-secret-dev
