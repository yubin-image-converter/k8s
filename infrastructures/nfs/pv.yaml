apiVersion: v1
kind: PersistentVolume
metadata:
  name: uploads-pv
spec:
  capacity:
    storage: 30Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: $(NFS_SERVER)
    path: $(NFS_PATH)
  persistentVolumeReclaimPolicy: Retain
